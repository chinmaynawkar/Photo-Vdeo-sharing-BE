"""
Routers package: each module defines an APIRouter for a domain.

Include in the main app with:
    from app.routers.files import router as files_router
    app.include_router(files_router, tags=["Files"])
"""

